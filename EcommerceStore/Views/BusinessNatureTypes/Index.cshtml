@model IEnumerable<EcommerceStore.Models.BusinessNatureType>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.TypeName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Products)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(model => item.TypeName)
            </td>
            <td>
                @Html.DisplayFor(model => item.Products)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.NatureTypeID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.NatureTypeID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.NatureTypeID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
